extends ../_templates/player
include assets/side-nav-content

append scripts
  script.
    let script = !{JSON.stringify(script).replace(/\{\{主人公\}\}/g, (user ? user.username : 'Master'))};
    const files = !{JSON.stringify(files)};
    const SCENARIOS = "#{SCENARIOS}";
  script(src='//cdn.jsdelivr.net/npm/howler@2.1.2/dist/howler.core.min.js')
  script(src='/js/player/scenario.js')

block content
  div(class='scenario panel-wrapper my-1')
    div.panel(sequence=0)
      button(class='invi leftNav', nav='left')
      |
      button(class='invi rightNav', nav='right')
      div#image.animate
      div#dialog
        div#text(data=0)
          div#characterName
          div#characterTalk
